machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t--rm=false auth0-express-api-rs256 . 

test:
  override:
    - docker run --env-file .env -p 3010:3010 -it auth0-express-api-rs256
    # - sh exec.sh
    - npm test
