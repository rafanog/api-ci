machine:
  services:
    docker

test:
  override:
    docker run auth0-express-api-rs256
    curl --retry 10 --retry-delay 5 -v http://localhost:3010

deployment:
  staging:
    branch: master
    heroku:
      appname: prueba-circleci
