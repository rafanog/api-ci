version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7.10
      
    working_directory: ~/repo

    steps:
      - checkout
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run: npm install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}
      
      - run:
          name: prepare backbround server
          command: echo "{AUTH0_DOMAIN}"
            # echo AUTH0_DOMAIN=rntest0116.auth0.com >> .env
            # echo AUTH0_AUDIENCE=https://localhost:3001/api >> .env
            
      #- run: 
      #    name: background server for tests
      #    command: npm start 
      #    background: true
      
      #- run: sleep 7
      
      #- run: 
      #    name: test
      #    command: npm test
